{
  "name": "learning-progress-tracker",
  "version": "1.0.0",
  "description": "The Learning Progress Tracker is a web application for structured tracking of your learning process. Users can view a list of predefined learning content, set their current learning status for each item, and add personal notes. Progress is clearly visualized, learning content can be searched or filtered, and all data is stored persistently so the learning state is preserved across visits.",
  "main": "index.js",
  "scripts": {
    "testui": "playwright test --ui",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "start": "npm install && node server/server.js",
    "dev": "npm install && nodemon server/server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Umex10/Lernfortschritts-Tracker.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "bugs": {
    "url": "https://github.com/Umex10/Lernfortschritts-Tracker/issues"
  },
  "homepage": "https://github.com/Umex10/Lernfortschritts-Tracker#readme",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "nodemon": "^3.1.11"
  }
}